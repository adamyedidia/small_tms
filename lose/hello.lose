nat r;
nat f;
nat g;

func myFunc(x, y, z) {
    if (x + 2) {
        y = (y > 3) + 2;
    }
    
    while(y){x=x+1;}
    
    ifelse ((z+3)<2) {
        z = 3;
    } {
        y = 5;
    }
    
    return;
}

x = f;

myFunc(r, f, g);

halt;