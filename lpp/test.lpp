     /*       if ((((a @ i) < (b @ j)) & ((c @ i) >= (d @ j))) | 
                (((a @ i) >= (b @ j)) & ((c @ i) < (d @ j)))) {
                
                print a;
            } */
            
            if ((a @ i) < (b @ j))
                           {
                
                            print a;
                        }